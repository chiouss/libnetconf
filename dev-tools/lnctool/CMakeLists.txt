install(PROGRAMS lnctool DESTINATION "${CMAKE_INSTALL_BINDIR}")
install(DIRECTORY rnglib xslt DESTINATION "${CMAKE_INSTALL_DATADIR}/${CMAKE_PROJECT_NAME}")
install(DIRECTORY generator/ DESTINATION "${CMAKE_INSTALL_DATADIR}/${CMAKE_PROJECT_NAME}/templates")
